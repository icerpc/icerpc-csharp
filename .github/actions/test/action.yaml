name: Run Tests

runs:
  using: "composite"
  steps:
  - name: 🧪 Test Slice Compiler
    run: cargo test --manifest-path tools/slicec-cs/Cargo.toml
    shell: bash
  - name: 🧪 Test
    # See https://github.com/microsoft/vstest/issues/2080#issuecomment-539879345
    run: dotnet test --no-build --verbosity normal --logger:"console;noprogress=true" --diag ${{ github.workspace }}/diagnostics.log
    shell: bash
  - name: Upload diagnostic log
    uses: actions/upload-artifact@v4
    with:
      name: diagnostics-${{ runner.os }}
      path: ${{ github.workspace }}/diagnostics.log
      if-no-files-found: ignore
    if: failure()
  - name: Upload hang dumps
    uses: actions/upload-artifact@v4
    with:
      name: hang-dumps-${{ runner.os }}
      path: ${{ github.workspace }}/**/*.dmp
      if-no-files-found: ignore
    if: failure()
