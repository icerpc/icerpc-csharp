{
  "Logging": {
    "Console": {
      "IncludeScopes": true,
      "LogLevel": {
        "Default": "Trace"
      }
    }
  },
  "AppSettings": {
    //"Hello.Endpoints": "tcp -h localhost -p 10000"
    "Hello.Endpoints": "ice+tcp://localhost:10000"
  }
}
