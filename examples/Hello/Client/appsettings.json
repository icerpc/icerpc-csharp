{
  "Logging": {
    "Console": {
      "IncludeScopes": true,
      "LogLevel": {
        "Default": "Trace"
      }
    }
  },
  "AppSettings": {
    //"Hello.Proxy": "hello:tcp -h localhost -p 10000"
    "Hello.Proxy": "ice+tcp://localhost:10000/hello"
  }
}
