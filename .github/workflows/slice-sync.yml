name: "Verify vendored Slice files"

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]

jobs:
  verify-slice:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.13"

      - run: python tools/sync-slice.py --verify
