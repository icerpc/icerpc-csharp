// Copyright (c) ZeroC, Inc. All rights reserved.

module Demo;

interface Hello
{
    // Setting the compress attribute instructs the slicec-cs compiler to add the `ICompressFeature` to the features of
    // the request, the deflate interceptor and middleware will apply the deflate algorithm to the payload of a request 
    // or response when this request feature is present.
    [compress(Args, Return)] sayHello(name: string) -> string;
}
